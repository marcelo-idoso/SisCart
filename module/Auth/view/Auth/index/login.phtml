<?php
    $form = $this->form;
    
    if ($this->error){
       echo '<div class="alert alert-danger">' . $this->error[0] . '
            <button type="button" class="close" data-dismiss="alert">x</button>)
       </div>'; 
    }
    var_dump($this->flashMessenger()->getErrorMessages());
    
    echo $this->flashMessenger()
         ->setMessageOpenFormat('<div%s>')
         ->setMessageSeparatorString('<p></p>')
         ->setMessageCloseString('<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button></div>')
         ->render('error' , array('alert alert-danger'));
    
    
    
    $form->setAttribute('action', $this->url('login' , array('controller' => 'Auth' , 'action' => 'login')));
    
    $form->prepare();
    echo $this->form()->openTag($form);
    echo $this->formCollection($form);
    echo $this->form()->closeTag($form);
    
    
    
?>

<style type="text/css">
    input, label{
        clear: both;
        display: block;
    }
</style>