<div class="jumbotron">
<?php
    $this->flashMessenger()
         ->setMessageOpenFormat('<div%s>')
         ->setMessageSeparatorString('<p></p>')
         ->setMessageCloseString('<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>')
         ->render('error' , array('alert alert-danger'));
    
    $form = $this->form;
    $form->setAttribute('class', 'form-horizontal form-label-left' , 'action', $this->url('login' , array('controller' => "auth" , 'action' => 'login')));
    
    $form->prepare();
    echo $this->form()->openTag($form);
    echo $this->formCollection($form);
    echo $this->form()->closeTag();
    
?>
</div>
<style type="text/css">
    input, label{
        clear: both;
        display: block;
    }
</style>